import {showHideBlock} from '../helpers';

var Workspace = Backbone.Router.extend({

	routes: {
		"": "main",    // #main
		"help": "help"  // #help
		//"search/:query/p:page": "search"   // #search/kiwis/p7
	},

	main: function() {

	},

	help: function() {

	}

});